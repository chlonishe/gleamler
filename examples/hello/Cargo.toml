[package]
name = "hello"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
gleamler = { path = "../../gleamler", default-features = false, features = ["nif_version_2_17"] }
